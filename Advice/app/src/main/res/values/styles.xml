<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    
    <style name="FaceBookLoginButton">
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:alpha">0.70</item>
        <item name="android:background">@drawable/facebook_button</item>
        <item name="android:drawableLeft">@drawable/facebook_icon</item>
        <item name="android:paddingLeft">2dp</item>
        <item name="android:text">@string/facebook_sign_in</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
